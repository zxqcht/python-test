def pri(n):
    sum = 0
    for z in range(2, n + 1):
        c = 0
        for i in range(2, z):
            if z % i == 0:
                c = c + 1
        if c == 0:
            sum = z + sum
    print(sum)


x = int(input('请输入一个整数n:'))
pri(x)
